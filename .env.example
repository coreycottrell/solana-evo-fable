# Fable / v2 A/B arm — paper only. Never points at v1 data/.
EVO_BOT_V2_DATA_DIR=data
# Alias also accepted:
# EVO_FABLE_DATA_DIR=data

# Shared OpenRouter key is still Jev-only (allowlisted Decisions).
# Prefer symlink: ln -sfn ../solana-evo-bot/.env .env  (gitignored)
OPENROUTER_API_KEY=
# Mock is OFF when key is present. Force mock with:
# EVO_BOT_JEV_MOCK=1
# Force live (requires key):
# EVO_BOT_JEV_MOCK=0
